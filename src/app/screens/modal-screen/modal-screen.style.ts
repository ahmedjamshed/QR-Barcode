import { StyleSheet, ViewStyle } from "react-native"

export const ModalScreenStaticStyle = StyleSheet.create({
  ROOT: {
    flex: 1,
  } as ViewStyle,

  ADD: {
    flexDirection: "row",
  } as ViewStyle,
})
